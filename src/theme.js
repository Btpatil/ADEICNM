import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette:{
        primary:{
            black: "black",
            light: "white"
        },
        secondary:{
            main: "green"
        },
        grey: {
            main:'grey'
        }
    }
})